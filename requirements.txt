# Core NeMo Curator - GPU Enabled
# Updated: 2026-01-05 - Installed GPU-enabled version with CUDA 12.x support
# Note: Version 0.9.0 required for notebook compatibility (1.0.0 has breaking changes)
nemo-curator==0.9.0

# GPU-Accelerated Data Processing (RAPIDS)
# Install with: --extra-index-url=https://pypi.nvidia.com
cudf-cu12>=25.12.0
dask-cudf-cu12>=25.12.0
dask-cuda>=25.12.0
cugraph-cu12>=25.12.0  # Required for fuzzy deduplication

# PII Detection
presidio-analyzer

# Jupyter Environment
jupyter>=1.0.0
jupyterlab>=4.0.0
notebook>=7.0.0
ipykernel>=6.0.0

# Data Processing
# Note: Specific dask/distributed versions required by RAPIDS
dask==2025.9.1
distributed==2025.9.1
#
# NumPy 2.x breaks some 3rd-party libs used by this repo (notably `fasttext`)
# due to stricter handling of `copy=False`. Keep NumPy <2 for now.
numpy<2
pandas>=2.0.0

# ML Dependencies
transformers>=4.30.0
torch>=2.0.0
scikit-learn>=1.3.0

# Language Detection (fasttext model downloaded separately)
# Model: https://dl.fbaipublicfiles.com/fasttext/supervised-models/lid.176.bin

# Installation Notes:
# For GPU support, install with:
# pip install -r requirements.txt --extra-index-url=https://pypi.nvidia.com
#
# Compatibility: Requires CUDA 12.x and compatible NVIDIA GPU
# Tested on: RTX 4060 8GB


